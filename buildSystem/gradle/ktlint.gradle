apply plugin: "org.jlleitschuh.gradle.ktlint"

ktlint {
    version = "1.8.0"
    debug = true
    reporters {
        reporter "plain"
        reporter "checkstyle"
        reporter "html"
    }
    outputColorName = "RED"
}
