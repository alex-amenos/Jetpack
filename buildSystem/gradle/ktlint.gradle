apply plugin: "org.jlleitschuh.gradle.ktlint"

ktlint {
    debug = true
    reporters {
        reporter "plain"
        reporter "checkstyle"
        reporter "html"
    }
    outputColorName = "RED"
}
